schema_version = "1.0.0"
id = "local_asset_manager"
version = "2.1.0"
name = "Local Asset Manager"
tagline = "Professional asset management with support for thousands of assets"
maintainer = "alfa haliza <alfa.haliza@example.com>"
type = "add-on"

# Website and Support
website = "https://github.com/huhatrash/asset_manager_blender"

# Blender Compatibility (Must be a string)
blender_version_min = "4.0.0"

# License (Must be a list of strings)
license = [
  "GPL-2.0-or-later",
]

# Metadata
[metadata]
tags = ["3D View", "Asset Management", "Productivity"]
platforms = ["windows", "macos", "linux"]
